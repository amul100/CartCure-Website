# CartCure Website

## Project Info
- **Repository**: https://github.com/amul100/CartCure-Website
- **Local Path**: c:\Users\andre\OneDrive\Documents\CartCure\Cartcure-website
- **Main Branch**: main

## Git Commands
**Important**: On Windows, bash output may not display. Just run the commands directly without waiting for output confirmation.

```bash
# Stage, commit, and push in one command:
git add . && git commit -m "Your commit message" && git push origin main
```

Or separately:
```bash
git add .
git commit -m "Your message"
git push origin main
```
